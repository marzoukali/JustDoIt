namespace TIS.Todo.Domain.Interfaces
{
    /// <summary>
    ///     Can be user to extend ToDosRepository in case we need behaviors other than the ones in GenericRepository
    /// </summary>
    public interface IToDosRepository
    {
    }
}